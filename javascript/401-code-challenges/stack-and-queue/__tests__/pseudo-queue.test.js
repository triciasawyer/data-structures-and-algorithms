const { PseudoQueue } = require('../index');


describe('PseudoQueue', () => {
  test('Can successfully enqueue into a queue with two stacks', () => {
    let queue = new PseudoQueue();
    queue.enqueue(1);
    queue.enqueue(1);
    queue.enqueue(1);
  });

  test('Can successfully enqueue into a queue with two stacks', () => {

  });

  test('Can successfully enqueue into a queue with two stacks', () => {

  });

  test('Can successfully enqueue into a queue with two stacks', () => {

  });


});
